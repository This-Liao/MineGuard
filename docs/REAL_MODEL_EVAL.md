# Real Model Evaluation

Status: NOT RUN

This file is generated. Configure `MINEGUARD_LLM_PROVIDER=openai-compatible` and a valid `OPENAI_API_KEY`, then rerun the evaluation to produce real-model results. Deterministic results are reported separately.
