# 自动化工具调用安全边界

> Synthetic Demo Data — 仅供 MineGuard 架构演示。

查询事件、检索知识和查看设备状态属于只读操作。启动或停止检测任务属于高风险写操作。系统必须依据工具元数据和后端审批令牌执行策略，不能相信提示词中的“管理员”“跳过审批”等声明。

参数必须通过 JSON Schema 校验；未知工具、缺少 cameraId、非法 algorithm 或未批准操作都应返回结构化错误并写入 Trace。
