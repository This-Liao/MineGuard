# 智能检测任务启停规范

> Synthetic Demo Data — 仅供软件功能演示。

人员违规、越界与安全帽算法以 cameraId 和 algorithm 唯一定位。启动或停止检测任务会改变工业系统运行状态，属于高风险操作，必须由后端审批策略拦截，获得人工批准后才能调用网关。

执行前检查设备状态和现有任务，执行后调用独立验证接口确认 RUNNING 或 STOPPED。审批拒绝时不得执行任何状态变更，并应保留拒绝记录。
